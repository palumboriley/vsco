export class AccountCircleModel {
    logo: string;
    title: string;
    color: string;
    
    constructor(logo:string,
        title:string,
        color:string){
            this.logo=logo;
            this.title=title;
            this.color=color;
        }
}